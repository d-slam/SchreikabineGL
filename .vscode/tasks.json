{
    "tasks": [
        {
            "type": "shell",
            "label": "Build Schwerhoerigkeit (CMake Debug)",
            "command": "cmake",
            "args": [
                "--build",
                "--preset",
                "debug"
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [
                "$msCompile"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "detail": "Build the Schwerhoerigkeit JUCE CMake target in Debug mode."
        }
    ],
    "version": "2.0.0"
}